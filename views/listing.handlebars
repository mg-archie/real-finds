<h3>Listing page</h3>

<!--<div class="flex gap-4 overflow-auto">
  {{#each listings}}
  <div class="rounded-lg p-4 my-2 shadow-sm shadow-indigo-100">
    <a href="/listing/{{this.id}}">
      <img alt="Home" src="/images/frames-for-your-heart-mR1CIDduGLc-unsplash.jpg"
        class="h-56 w-full rounded-md object-cover" />

      <div class="mt-2">
        <div class="text-sm text-gray-500">${{this.price}}</div>
        <div class="font-medium">{{this.city}}, {{this.postal_code}}</div>
        <div class="text-sm text-gray-500">{{this.listing_type}}</div>

        <div class="mt-6 flex items-center gap-8 text-xs">
          <div class="sm:inline-flex sm:shrink-0 sm:items-center sm:gap-2">
            <svg class="h-4 w-4 text-indigo-700" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
              stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M8 14v3m4-3v3m4-3v3M3 21h18M3 10h18M3 7l9-4 9 4M4 10h16v11H4V10z" />
            </svg>
            <div class="mt-1.5 sm:mt-0">
              <p class="text-gray-500">Sqf</p>
              <p class="font-medium">{{this.rooms}}</p>
            </div>
          </div>

          <div class="sm:inline-flex sm:shrink-0 sm:items-center sm:gap-2">
            <svg class="h-4 w-4 text-indigo-700" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
              stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
            </svg>
            <div class="mt-1.5 sm:mt-0">
              <p class="text-gray-500">Baths</p>
              <p class="font-medium">{{this.baths}}</p>
            </div>
          </div>

          <div class="sm:inline-flex sm:shrink-0 sm:items-center sm:gap-2">
            <svg class="h-4 w-4 text-indigo-700" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
              stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
            </svg>
            <div class="mt-1.5 sm:mt-0">
              <p class="text-gray-500">Rooms</p>
              <p class="font-medium">{{this.rooms}}</p>
            </div>
          </div>
        </div>
      </div>
    </a>
  </div>
  {{/each}}
</div>-->
<div class ="listings">
     <a href="/listing/{{this.id}}">
    
    <h4>Listing Price: {{this.price}}</h4>
    <h5>Address: {{this.address}}, {{this.postal_code}}, {{city}}</h5>
    <h6>Property Type: {{this.listing_type}}</h6>
    <h7># of Rooms: {{this.rooms}}, # of Baths: {{baths}}</h7>
</div>

      <div class="mt-2">
        <div class="text-sm text-gray-500">${{this.price}}</div>
        <div class="font-medium">{{this.city}}, {{this.postal_code}}</div>
        <div class="text-sm text-gray-500">{{this.listing_type}}</div>
    </a>

